# =============================================================================
# FRONTEND ENVIRONMENT CONFIGURATION
# =============================================================================
# Copy this file to .env and fill in your actual values
# DO NOT commit .env files to version control

# =============================================================================
# API CONFIGURATION
# =============================================================================
# Backend API base URL (without /api/v1 suffix)
# Development: http://localhost:5000
# Production: https://your-backend-domain.com
VITE_API_BASE_URL=http://localhost:5000

# =============================================================================
# GOOGLE SERVICES
# =============================================================================
# Google Maps API Key for location services and map components
# Get your API key from: https://developers.google.com/maps/documentation/javascript/get-api-key
# Required APIs: Maps JavaScript API, Places API (if using place search)
VITE_GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================
# Application environment (development, production, test)
NODE_ENV=development

# Application name and branding
VITE_APP_NAME=Marcel Expenses
VITE_APP_VERSION=1.0.0

# =============================================================================
# DEVELOPMENT CONFIGURATION
# =============================================================================
# Enable development features and debugging
VITE_DEBUG_MODE=true

# Enable detailed error logging in development
VITE_ENABLE_LOGGING=true

# =============================================================================
# SECURITY NOTES
# =============================================================================
# 1. All Vite environment variables must be prefixed with VITE_ to be accessible in the browser
# 2. Never expose sensitive backend secrets in frontend environment variables
# 3. Frontend environment variables are visible to users in the browser
# 4. Use different API URLs for different environments
# 5. Regularly rotate API keys and monitor usage
# 6. Set up proper CORS configuration on the backend

# =============================================================================
# QUICK SETUP GUIDE
# =============================================================================
# 1. Copy this file: cp .env.example .env
# 2. Get Google Maps API key from Google Cloud Console
# 3. Update VITE_API_BASE_URL to match your backend server
# 4. Set NODE_ENV to 'production' for production builds
# 5. Test the configuration: npm run dev

# =============================================================================
# EXAMPLE CONFIGURATIONS
# =============================================================================
# Development:
# VITE_API_BASE_URL=http://localhost:5000
# NODE_ENV=development
# VITE_DEBUG_MODE=true
#
# Production:
# VITE_API_BASE_URL=https://api.marcel-expenses.com
# NODE_ENV=production
# VITE_DEBUG_MODE=false